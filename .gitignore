# agent / editor local state
.claude/
.codegraph/

# graphify regenerable cache (AST cache + stat-index) — rebuilt on every graph
# update; tracking it churned the tree. Graph outputs stay tracked.
graphify-out/cache/

# node / build (app has its own too)
node_modules/
dist/
out/
*.log

.DS_Store
Thumbs.db
