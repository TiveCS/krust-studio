**Krust Studio v1.6.0** — feature release.

Inline filter with a raw-WHERE escape hatch, pinned (frozen) columns, a visual query plan, pinnable/reorderable tabs, a Postgres schema selector, colour-coded foreign keys, and a much snappier data grid (DOM-overlay drag-select + configurable virtualization).

📦 `krust-studio-app-1.6.0-setup.exe` below. SmartScreen may warn (unsigned) → **More info → Run anyway**.
