**Krust Studio v1.3.0** — your tabs now persist across restarts, connections survive idle drops, and there's built-in **Backup / Restore**.

Highlights: persistent per-connection+database workspace · transparent idle-drop auto-retry + manual reconnect · self-contained `.sql` backup/restore with a destructive-statement dry-run · "Referenced by" reverse-FK sub-tab + walkable FK graph · everything-is-a-tab · custom title bar.

📦 `krust-studio-app-1.3.0-setup.exe` below — existing installs auto-update.
