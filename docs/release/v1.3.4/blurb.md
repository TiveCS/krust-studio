**Krust Studio v1.3.4** — structure-editor fixes.

Pending schema edits no longer disappear when you switch tabs, dropping a foreign-key column no longer errors on MySQL, and tabs now show an unsaved-changes dot with a confirm-before-close (plus bulk close: others / right / all).

📦 `krust-studio-app-1.3.4-setup.exe` below. SmartScreen may warn (unsigned) → **More info → Run anyway**.
