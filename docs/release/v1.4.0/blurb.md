**Krust Studio v1.4.0** — feature release.

Virtualized data grid (smooth on huge tables), drop foreign keys from the Relations tab, Backup & Restore as a full tab, syntax-highlighted Query History with expand + format, and local reusable table templates (id + audit columns in one click).

📦 `krust-studio-app-1.4.0-setup.exe` below. SmartScreen may warn (unsigned) → **More info → Run anyway**.
